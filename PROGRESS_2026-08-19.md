# สรุปความคืบหน้า — หน้าใบแจ้งเรื่อง (outbox / inbox)

**วันที่:** 19 สิงหาคม 2026
**ขอบเขต:** หน้า "เรื่องที่แจ้งออกไป" + "เรื่องแจ้งเข้ามา" และการกดดำเนินการทุกขั้นของ workflow
**สถานะรวม:** ฝั่งหน้าเว็บ **ต่อครบทุกขั้นแล้ว** — ยังทดสอบกับข้อมูลจริงไม่ได้ (ดู §5)

---

## 1. โจทย์ตั้งต้น

แผนกหนึ่งแจ้งเรื่องไปได้ทุกแผนก แต่ **แต่ละแผนกมี workflow ไม่เท่ากัน** (2–6 ขั้น)
และ **รหัสสถานะ (`JobStatus`) คนละชุด ความหมายไม่ตรงกัน**

คำถามที่ต้องตอบให้ได้: *"ตอนนี้มีเรื่องรออนุมัติกี่ใบ รอดำเนินการกี่ใบ รอประเมินกี่ใบ"*
ทั้งที่ใบเหล่านั้นมาจากคนละแผนกที่นับสถานะคนละแบบ

### หลักฐานว่า `JobStatus` ใช้นับข้ามแผนกไม่ได้

ตรวจจากตาราง `BC_<แผนก>_WorkFlow` จริง 9 ชุด (AF / GA / IM / IT / PL / PS / SQA + แผนก `11`, `30`)

| ปัญหา | หลักฐาน |
|---|---|
| รหัสเดียวกัน คนละความหมาย | `SQA` JobStatus **1** = รอรับเรื่อง แต่แผนกอื่น **1** = รอ Mgr อนุมัติ |
| รหัสซ้ำในใบเดียวกัน | `PS` step 2 กับ 3 เป็น **2** ทั้งคู่ / step 4 กับ 5 เป็น **3** ทั้งคู่ |
| ความหมายไม่เท่ากัน | `IT` **3** = `Service` (เหลืออีก 2 ขั้น) / `AF`,`PL` **3** = `Service And Close-Job` (เสร็จแล้ว) |
| รหัส "ปิดงาน" ไม่ตรงกัน | จบที่ JobStatus สูงสุดคนละตัว: SQA=2, GA/IM=3, PS=4, IT/AF/PL=5 |

### ทางออกที่ใช้

`WFStatus` ในฐานข้อมูลเป็น **ศัพท์กลางอยู่แล้ว** — ทั้ง 9 workflow ใช้ร่วมกันแค่ **11 ค่า**
ต่างกันแค่เลือกใช้ตัวไหนและเรียงยังไง

→ backend map `WFStatus` → **`phase`** (8 ค่า) แล้วส่งมาต่อ item + `phaseSummary`
→ หน้าเว็บนับ/ให้สีจาก `phase` อย่างเดียว **ไม่รู้จัก `WFStatus` ของแผนกไหนเลย**

| `phase` | ป้ายไทย |
|---|---|
| `waiting_approve` | รออนุมัติ |
| `waiting_accept` | รอปลายทางรับเรื่อง |
| `in_progress` | กำลังดำเนินการ |
| `waiting_review` | รอตรวจรับ / ประเมิน |
| `waiting_close` | รอปิดงาน |
| `closed` / `cancelled` / `other` | ปิดงานแล้ว / ยกเลิก / อื่น ๆ |

---

## 2. ที่ทำเสร็จแล้ว (ฝั่งหน้าเว็บ)

### 2.1 หน้า "เรื่องที่แจ้งออกไป" (`/my`)

- **ตารางเดียวรวมทุกแผนกปลายทาง** — เรียก `/Requests/outgoing` โดยไม่ระบุ `module`
- **การ์ด KPI ตามจังหวะงาน** สร้างจาก `phaseSummary` ที่ API ส่งมา คลิกเพื่อกรองที่ server
  - แผนกที่ไม่มีขั้นนั้นจะ**ไม่มีการ์ดโผล่มา** (GA/IM ไม่มีขั้นรอปิดงาน, มีแต่ IT ที่มี Survey)
  - `phaseSummary` ไม่เปลี่ยนตามตัวกรอง → badge ของการ์ดอื่นยังบอกตัวเลขจริง
- **ปุ่ม "รอเราลงมือ"** — หลาย workflow วนกลับมาหาผู้แจ้งตอนท้าย (`Survey` / `Received-Service` /
  `Request-Close-Job`) ใบที่งานเสร็จแล้วรอแค่เรากดปิดจะค้างเงียบถ้าไม่มีตัวชี้นี้
- คอลัมน์ใหม่: **แจ้งไปที่** (module), **จังหวะงาน** (phase), **คิวเรา**, ขั้นที่ "3 จาก 5"

### 2.2 หน้า "เรื่องแจ้งเข้ามา" (`/inbox`)

คิวงานจริงของแผนกปลายทาง — ได้การ์ดจังหวะงานแบบเดียวกัน + ปุ่ม "เฉพาะคิวของเรา"

### 2.3 การกดดำเนินการ — ครบทั้ง workflow

ปุ่มทุกปุ่มมาจาก `item.availableActions` ที่ API ส่งมา **ไม่มี hardcode ต่อแผนก/ต่อขั้น**

```
step 1  Approved-Request      Mgr ต้นสังกัด   →  approve
step 2  Received-Service      IT              →  receive / skipService
step 3  Service               IT              →  service
step 4  Survey                ผู้แจ้ง          →  survey
step 5  ReceiveJob-Close-Job  Mgr ต้นสังกัด   →  close  →  ปิดใบ
```

**ฟอร์มวาดจาก `requiredFields` อัตโนมัติ:**

| ปุ่ม | ฟอร์มที่ขึ้น |
|---|---|
| รับเรื่อง | กล่องยืนยันเปล่า |
| ปิดรับเรื่อง (ไม่ดำเนินการ) | 4 ช่องบังคับ + กลุ่มพับ "ส่งซ่อมภายนอก / รออะไหล่" |
| ดำเนินการเสร็จ | เหมือนกัน |
| ส่งผลประเมิน | ปุ่มระดับ 5 ระดับ + ช่องคะแนน 1–25 โชว์ % สด |
| ปิดงานรับเรื่อง | กลุ่มพับ "ข้อมูลปิดงาน" (เคส / ผล KPI) |

---

## 3. การตัดสินใจที่บันทึกไว้

| # | เรื่อง | เหตุผล |
|---|---|---|
| 1 | **ไม่มีปุ่มดำเนินการในแถวตาราง** — ทุก action อยู่ในหน้ารายละเอียดเท่านั้น | คนอนุมัติต้องเปิดอ่านก่อนว่าลูกน้องขออะไร กดจากแถวตารางไม่ใช่การตัดสินใจจริง (ปุ่มตาของแถวที่กดได้จะเน้นสีไว้ให้หาเจอ) |
| 2 | ตารางรวมทุกแผนก ไม่แยกแท็บ | ผู้ใช้ถามว่า "เรื่องที่แจ้งไปถึงไหนแล้ว" ไม่ใช่ "เรื่องของแผนก IT" — แยกแท็บแล้วใบที่ค้างนานสุดจะอยู่แท็บที่ไม่ได้เปิด |
| 3 | สีชิปสถานะมาจาก `phase` ไม่ใช่ `jobStatus` | รหัสเดียวกันคนละความหมายในแต่ละแผนก |
| 4 | `phase` ที่ไม่รู้จัก → การ์ด **"อื่น ๆ"** ห้ามเดาเป็น `closed` | นับใบที่ยังค้างเป็น "เสร็จแล้ว" คือความผิดพลาดที่ไม่มีใครจับได้ |
| 5 | คีย์แถว = `module::docNo` | `docNo` ซ้ำข้ามแผนกได้ (แต่ละแผนกออกเลขเอง) — **แก้บั๊กที่ทำให้เปิดใบผิด** |
| 6 | ค่าว่างไม่ถูกส่งไปกับ `fields` | API ถือว่า "ฟิลด์ที่ไม่ส่ง = คงค่าเดิม" ส่ง `""` ไปคือการล้างค่าเดิมโดยไม่ตั้งใจ |
| 7 | ซ่อนช่อง "หมายเหตุ" จนกว่า `requireNote: true` | backend ยังไม่มีคอลัมน์เก็บ `note` — ให้พิมพ์แล้วข้อมูลหายเงียบ ๆ แย่กว่าไม่มีช่อง |
| 8 | ยืนยันก่อนทุก action | ทุกขั้นเปลี่ยนสถานะจริงและย้อนกลับไม่ได้ |

---

## 4. ไฟล์ที่เปลี่ยน

**ใหม่**

| ไฟล์ | หน้าที่ |
|---|---|
| `src/data/requestPhase.ts` | สี/ลำดับ/ป้ายของ phase (**presentation เท่านั้น** — ตาราง map อยู่ที่ backend) |
| `src/data/requestActionFields.ts` | ⚠️ SHIM: ชื่อฟิลด์ → label/type/options + กลุ่มฟิลด์ไม่บังคับ |
| `src/hooks/useRequestAction.ts` | POST action + แปลง `message` จาก API เป็นข้อความให้ผู้ใช้ |
| `src/components/items/RequestActionDialog.tsx` | กล่องยืนยัน + ฟอร์มที่วาดจาก `requiredFields` |

**แก้**

`src/api/client.ts` (เพิ่ม `apiSend` + `ApiError` ที่ดึง `message` ไทยจาก body) ·
`src/api/requests.ts` · `src/hooks/useRequestList.ts` · `src/types/requestList.ts` ·
`src/data/requestListData.ts` · `src/components/items/RequestGrid.tsx` ·
`src/components/items/RequestDetailModal.tsx` · `src/pages/MyItems.tsx` · `src/pages/Inbox.tsx` ·
`CLAUDE.md` · `API_SPEC_REQUESTS_V2.md` · `.env.example`

**ลบ** — `src/hooks/useOutgoingRequests.ts` (fan-out ทีละ module ไม่ต้องใช้แล้ว หลัง API รับ `module` ว่างได้)

**สถานะโค้ด:** `tsc --noEmit` ผ่าน · dev server คอมไพล์ไม่มี error · ยังไม่ commit

---

## 5. 🔴 ติดอยู่ตรงนี้ — ยังทดสอบกับข้อมูลจริงไม่ได้

ทดสอบล่าสุด **ปุ่มอนุมัติไม่ขึ้น** เพราะ API ส่ง `availableActions: []` มา
ตอนนั้นเจอ 2 เรื่องซ้อนกัน:

1. **VPN หลุด** → `POST /Auth/login` ตอบ 500 `"An unexpected error occurred."`
   (ยืนยันว่าไม่ได้มาจากหน้าเว็บ — `auth.ts` / `Login.tsx` / `config.ts` ไม่ถูกแก้เลย)
2. **โค้ด v2.1/v2.2 ที่ backend แก้ อาจยังไม่ได้รันอยู่** — ตรวจพอร์ตแล้วพบว่า
   `5192` และ `7241` ไม่มีอะไรฟัง มีแต่ `44377` (IIS) ซึ่งหน้าเว็บชี้ไปโดย default

**ต้องทำก่อนทดสอบรอบหน้า:**

- ยืนยันว่า API ที่รันอยู่เป็นเวอร์ชันล่าสุด (deploy ขึ้น IIS 44377 หรือ `dotnet run` ที่ 5192)
- ถ้าใช้ 5192 ต้องสร้าง `.env` แล้ว **รีสตาร์ท `npm start`** (CRA อ่าน `.env` ตอนบูตเท่านั้น)
  ```
  REACT_APP_API_BASE_URL=http://localhost:5192
  ```
- เช็คใน DevTools → Network → `outgoing` → ดูว่า `availableActions` มี `approve` มาไหม

---

## 6. รอจาก backend

### 6.1 บั๊กที่ยังไม่ได้แก้

**`phase` ของ IT step 2 ผิด** — v2.2 ระบุว่า step 2 `Received-Service` คือขั้น **"รับเรื่อง"** ของ IT
แต่ `phase` ยังเป็น `waiting_review` (จังหวะของ*ผู้แจ้ง*)

→ ใบที่รอ IT รับเรื่องจะไปกองในการ์ด **"รอตรวจรับ / ประเมิน"** ปนกับใบที่รอผู้แจ้งประเมินจริง
และการ์ด **"รอปลายทางรับเรื่อง"** จะว่างตลอดกาล

ควรเป็น `waiting_accept` — แก้ที่ backend จุดเดียว หน้าเว็บไม่ต้องแตะ

### 6.2 ขอเพิ่ม (เรียงตามความสำคัญ) — รายละเอียดเต็มใน `API_SPEC_REQUESTS_V2.md` §10–§11

| # | เรื่อง | ทำไม |
|---|---|---|
| 1 | **ส่ง `phoneNumber` / `comName` / `remark` มาใน list** | **มีใน DB แล้ว** แค่ไม่ได้ส่งออกมา — ตอนนี้เปิดใบมาอ่านได้แค่ `detail` ซึ่งบางใบมีแค่ "124" คนอนุมัติตัดสินใจไม่ได้ |
| 2 | **เก็บ `note` ลง DB** | เป็นเงื่อนไขของปุ่ม "ไม่อนุมัติ" และ log แบบ insert-only **ถ้าไม่เก็บตั้งแต่วันแรก ย้อนทำทีหลังไม่ได้** |
| 3 | **ปุ่ม "ไม่อนุมัติ"** (`not_approve` / `reject`) | Mgr ที่กดได้แค่ "อนุมัติ" ไม่ได้เรียกว่าอนุมัติ เพราะไม่มีทางเลือก — **หน้าเว็บไม่ต้องแก้โค้ด** แค่ใส่ลง `availableActions` |
| 4 | **metadata ของ `requiredFields`** | ตอนนี้ส่งมาแค่ชื่อฟิลด์ หน้าเว็บต้องเดาชนิด/ตัวเลือกเอง (เก็บไว้ที่ `requestActionFields.ts` ชั่วคราว) — ถ้าส่ง `{name,label,type,options,required}` มา ลบไฟล์ shim ทิ้งได้ |
| 5 | **`GET /Requests/{module}/{docNo}` + `logs`** | ตอนนี้ประวัติในหน้ารายละเอียดขึ้นบรรทัดเดียวคือ "แจ้งเรื่อง" — workflow ที่อนุมัติ 2 รอบ (เช่น PS) ต้องเห็นว่าใครอนุมัติขั้นก่อนหน้า |
| 6 | `/incoming` ให้รับ `module` ว่างได้ | ตอนนี้หน้า Inbox hardcode `module: 'IT'` ยังไม่พังเพราะมีโมดูลเดียว แต่วันที่เปิด AF/PL/GA คนแผนกอื่นจะไม่เห็นใบของตัวเอง |
| 7 | ไฟล์แนบ · `updatedDate` · อนุมัติหลายใบพร้อมกัน | ตามลำดับความสำคัญ |

### 6.3 คำถามที่ยังไม่มีคำตอบ

1. **หัวข้อสำรวจความพึงพอใจ 5 ข้อคืออะไร** — API รับคะแนนรวม 1–25 (5 หัวข้อ × 5 ระดับ)
   แต่ไม่ได้บอกหัวข้อ ตอนนี้หน้าเว็บทำเป็นระดับรวม 5 ปุ่ม (5/10/15/20/25) + ช่องใส่เลขเอง
   ถ้าได้หัวข้อจริงมา จะเปลี่ยนเป็น 5 แถว × 5 ดาว แล้วรวมคะแนนอัตโนมัติ
2. **`exVendor` คือฟิลด์อะไร เก็บคอลัมน์ไหน**
3. **รหัสแผนก `11` และ `30` คือแผนกอะไร** — ที่ระบุได้แล้ว: `03`=AF, `06`=GA, `07`=IT, `18`=IM,
   `20`=PL, `21`=PS, `29`=SQA แต่ยังไม่เห็น **SV** กับ **HR** ที่ระบุไว้ในเอกสาร
4. **ใบที่ปิด/ยกเลิกแล้วใช้ JobStatus อะไรในแต่ละแผนก** — IT ใช้ `6`/`9` แต่ GA (จบที่ 3) ใช้อะไร
5. **`SQA` ไม่มีขั้น `Approved-Request` เลย** → ขัดกับข้อตกลงใน `API_SPEC_REQUEST_FLOW.md`
   ที่ว่า "ทุกใบต้องให้ Mgr อนุมัติก่อน" — ยึดอันไหน

---

## 7. ขั้นถัดไป

1. ยืนยันว่า API เวอร์ชันล่าสุดรันอยู่ แล้วไล่ทดสอบทั้ง flow:
   อนุมัติ → รับเรื่อง → ดำเนินการเสร็จ → (สลับเป็นผู้แจ้ง) ส่งผลประเมิน → (Mgr) ปิดงานรับเรื่อง
   จบที่การ์ด "ปิดงานแล้ว"
2. ระหว่างทางให้ดู **การ์ด "อื่น ๆ"** — ถ้ามีเลขขึ้น แปลว่ามี `WFStatus` ที่ backend ยังไม่ได้ map
3. ดัน §6.2 ข้อ 1 (3 ฟิลด์ที่มีใน DB แล้ว) ให้ backend ก่อน — ถูกที่สุดและกระทบหน้าอนุมัติมากที่สุด
4. commit งานชุดนี้ (ยังไม่ได้ commit — ตอนนี้อยู่ใน working tree ทั้งหมด)
