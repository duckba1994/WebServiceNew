# คำขอเพิ่มเติมสำหรับ Requests API (v2)

> เอกสารนี้เขียนไว้ให้ **ก๊อปทั้งไฟล์ไปสั่งงาน backend ได้เลย**
> ต่อยอดจาก Requests API v1 ที่ทำเสร็จแล้ว (`/modules`, `/outgoing`, `/incoming`)
> ของเดิมไม่ต้องรื้อ — ทั้งหมดนี้เป็นการ "เพิ่ม" ไม่ใช่ "เปลี่ยน"

---

## 0. บริบทที่ทำให้ต้องมี v2

หน้าเว็บใช้ **หน้าจอเดียวกันกับทุกแผนก** โดยเปลี่ยนแค่ `module` แต่ตอนนี้เจอปัญหาว่า

**workflow ของแต่ละแผนกไม่เท่ากัน — บางแผนก 5 ขั้น บางแผนก 3 ขั้น ชื่อขั้นก็คนละชื่อ**

ถ้า frontend ต้องรู้เองว่าแผนกไหนมีกี่ขั้น ขั้นไหนชื่ออะไร ใครทำได้ ก็เท่ากับ hardcode workflow ของทุกแผนกไว้ในหน้าเว็บ
→ เปิดแผนกใหม่ = ต้องแก้โค้ด + deploy ใหม่ทุกครั้ง ซึ่งขัดกับเจตนาเดิมของ `/modules`

**หลักการที่ขอ: workflow เป็นข้อมูล ไม่ใช่โค้ด**
backend เป็นเจ้าของนิยาม workflow แล้วส่งมาให้ frontend เรนเดอร์ตามที่ได้รับ
เพิ่มแผนกใหม่ที่มี 7 ขั้น หน้าเว็บต้องแสดงได้ทันทีโดยไม่ต้องแก้อะไรเลย

---

## 1. ⭐ ให้ workflow ของแต่ละโมดูลออกมาเป็นข้อมูล

### 1.1 ขยาย `/modules` หรือเพิ่มเส้นใหม่
```
GET /api/v1/Requests/workflow?module=IT
```

```json
{
  "module": "IT",
  "name": "แจ้งเรื่อง IT",
  "stepCount": 5,
  "steps": [
    { "step": 1, "code": "Approved-Request", "name": "รอ Mgr หน่วยงานผู้แจ้งอนุมัติ", "ownerType": "requesterDepart" },
    { "step": 2, "code": "Receive",          "name": "รอแผนกปลายทางรับเรื่อง",      "ownerType": "targetDepart" },
    { "step": 3, "code": "Service",          "name": "อยู่ระหว่างดำเนินการ",         "ownerType": "targetDepart" },
    { "step": 4, "code": "Survey",           "name": "สำรวจความพึงพอใจ",            "ownerType": "requesterDepart" },
    { "step": 5, "code": "Close",            "name": "ปิดงาน",                      "ownerType": "targetDepart" }
  ],
  "statuses": [
    { "code": "1", "name": "แจ้งซ่อมแล้ว",            "group": "Open" },
    { "code": "2", "name": "รับเรื่องแจ้งซ่อมแล้ว",     "group": "Open" },
    { "code": "3", "name": "อยู่ระหว่างดำเนินการซ่อม",  "group": "Open" },
    { "code": "4", "name": "อยู่ระหว่างสำรวจความพึงพอใจ","group": "Open" },
    { "code": "5", "name": "อยู่ระหว่างปิดงาน",        "group": "Open" },
    { "code": "6", "name": "ปิดงานซ่อมแล้ว",          "group": "Closed" },
    { "code": "9", "name": "ยกเลิกรายการ",            "group": "Cancelled" }
  ]
}
```

**ทำไมต้องมี**
- `stepCount` + `steps` → หน้าเว็บวาดแถบความคืบหน้า "ขั้นที่ 2 จาก 5" และ timeline ได้กับทุกแผนก แผนก 3 ขั้นก็วาดเป็น 3 ขั้นเอง
- `statuses` → frontend เลิกฝังรายการสถานะไว้ในโค้ด (ตอนนี้ฝังของ IT ไว้ 7 รหัส ถ้าแผนกอื่นใช้รหัสอื่นจะแสดงผิด)
- `group` ต้องมาจาก backend เพราะเป็นตัวกำหนดว่าใบนั้นถือว่าปิดหรือยัง ซึ่งแต่ละแผนกอาจนิยามไม่เหมือนกัน

**ข้อกำหนด**
- ถ้ารวมเข้า `/modules` ได้ยิ่งดี (เรียกครั้งเดียวได้ทุกโมดูล) — `GET /Requests/modules?includeWorkflow=true`
- frontend จะ cache ไว้ตอนเปิดแอป จึงไม่ต้องห่วงเรื่องเรียกบ่อย
- **สีของสถานะไม่ต้องส่งมา** frontend เลือกสีจาก `group` เอง

### 1.2 เพิ่มใน items ของ list
| ฟิลด์ | ชนิด | ทำไมต้องมี |
|---|---|---|
| `wfStepTotal` | int | จำนวนขั้นทั้งหมดของ workflow ใบนั้น — แสดง "2/5" ในตารางได้โดยไม่ต้องรอโหลด workflow |
| `wfStepName` | string | ชื่อขั้นปัจจุบันแบบสั้น (ตอนนี้มีแต่ `description` ที่เป็นประโยคยาวปนชื่อแผนก ตัดใส่ตารางไม่สวย) |

---

## 2. ⭐ บอกมาว่า "ผู้ใช้คนนี้ทำอะไรกับใบนี้ได้บ้าง"

> ### ✅ ทำแล้วใน v2.0 / แก้สิทธิ์ใน v2.1
> `availableActions` มาครบทุก item แล้ว (มี `requiredFields` เพิ่มจากที่ขอ)
> หน้าเว็บเรนเดอร์ปุ่มจากตรงนี้ล้วน ๆ ไม่มี hardcode ต่อแผนก
>
> **ค้าง:** `requiredFields` บอกแค่ชื่อฟิลด์ (`repairStatus`, `exVendor`) ไม่ได้บอก
> ชนิด/ตัวเลือก/ป้ายไทย → action ที่มี `requiredFields` หน้าเว็บยัง**ปิดปุ่มไว้** ดู §10

ปัญหาเดียวกับข้อ 1: ปุ่มที่กดได้ในแต่ละขั้นของแต่ละแผนกไม่เหมือนกัน
ถ้า frontend ต้องคิดเองว่า "ขั้น 2 ของ IT กดรับเรื่องได้ ขั้น 2 ของ HR กดอย่างอื่น" ก็ hardcode อีก

**ขอให้ API ส่ง action ที่ทำได้มากับ item เลย**
```json
"availableActions": [
  { "code": "approve", "label": "อนุมัติ",     "style": "primary", "requireNote": false },
  { "code": "reject",  "label": "ไม่อนุมัติ",  "style": "danger",  "requireNote": true  }
]
```
- ถ้าผู้ใช้ทำอะไรไม่ได้ตอนนี้ → ส่ง array ว่าง `[]` (frontend ไม่แสดงปุ่ม)
- `style` รับค่า `primary` / `success` / `danger` / `neutral` — ใช้เลือกสีปุ่ม
- `requireNote: true` → frontend บังคับให้กรอกเหตุผลก่อนกดยืนยัน

แบบนี้เพิ่มแผนกใหม่ที่มีปุ่ม "ส่งซ่อมภายนอก" หน้าเว็บขึ้นปุ่มให้เองทันที ไม่ต้องแก้โค้ด

---

## 3. ⭐ endpoint สำหรับ "กดทำ"

> ### ✅ ทำแล้วใน v2.1 — เฉพาะ `approve`
> `POST /Requests/{module}/{docNo}/action` ใช้งานได้แล้ว หน้าเว็บต่อครบทั้ง
> outbox และ inbox (ปุ่มลัดในตาราง + ปุ่มในหน้ารายละเอียด + กล่องยืนยัน)
>
> **ค้าง:** `receive` / `service` / `survey` / `cancel` และ `note` ที่ยังไม่มีคอลัมน์เก็บใน DB
> — พอ action ไหนพร้อม หน้าเว็บขึ้นปุ่มให้เองทันทีโดยไม่ต้องแก้โค้ด
> (ยกเว้น action ที่มี `requiredFields` ดู §10)

```
POST /api/v1/Requests/{module}/{docNo}/action
{ "action": "approve", "note": "" }
```

- ตรวจสิทธิ์ใหม่ที่ backend ทุกครั้ง **ห้ามเชื่อว่า frontend ส่ง action ที่ถูกมา** (`availableActions` เป็นแค่ UX)
- ทำไม่ได้ (สถานะเปลี่ยนไปแล้ว / ไม่ใช่คิวของแผนกนี้ / ไม่มีสิทธิ์) → ตอบ **409** พร้อม `message` ภาษาไทยที่เอาไปแสดงให้ผู้ใช้อ่านได้เลย
- สำเร็จ → คืน item ตัวที่อัปเดตแล้วกลับมาทั้งก้อน (frontend จะได้ไม่ต้องโหลดลิสต์ใหม่ทั้งหน้า)
- ทุกครั้งที่สถานะเปลี่ยน **ต้องบันทึก log 1 แถว** (ใคร ทำอะไร เมื่อไร เหตุผลอะไร) แบบ insert อย่างเดียว ห้ามอัปเดตทับ

ถ้าไม่มีเส้นนี้ เว็บจะเป็นแค่จอดู ผู้ใช้ยังต้องกลับไปกดในโปรแกรมเดิมอยู่ดี

---

## 4. endpoint รายใบ + ประวัติ

```
GET /api/v1/Requests/{module}/{docNo}
```
คืนข้อมูลใบเต็ม + `logs` (ประวัติทุกครั้งที่สถานะเปลี่ยน เรียงเก่า→ใหม่) + `attachments`

```json
"logs": [
  { "step": 1, "action": "create",  "actionByName": "วิชิต เทียนทอง", "actionDate": "2026-06-15T09:00:00", "note": null },
  { "step": 1, "action": "approve", "actionByName": "หัวหน้าแผนก IT",  "actionDate": "2026-06-15T10:20:00", "note": null }
]
```

**เหตุผล**: ตอนนี้ list แบก `detail` (ยาวได้ 1000 ตัวอักษร) + `resolution` object เต็ม ๆ มาทุกแถว
ทั้งที่ผู้ใช้เปิดดูจริงแค่ใบเดียว — ที่ 77 ใบไม่รู้สึก ที่ 5,000 ใบคือหลาย MB ต่อการเปิดหน้าหนึ่งครั้ง
มีเส้นนี้แล้ว list จะตัด `detail` เหลือ 100 ตัวอักษรแรกและตัด `resolution` ออกได้

---

## 5. pagination — ขอใส่ parameter ไว้ตั้งแต่ตอนนี้

```
GET /Requests/incoming?module=IT&page=1&pageSize=50
```
- **default = คืนทั้งหมดเหมือนเดิม** (ไม่ส่ง page มาก็พฤติกรรมเดิมเป๊ะ ไม่กระทบใคร)
- คืน `page` / `pageSize` / `totalCount` / `totalPages` ใน response

**เหตุผล**: ตอนนี้มี consumer เจ้าเดียวคือหน้าเว็บนี้ การเติมทีหลังตอนมีคนใช้หลายที่ = breaking change ทุกจุด
ใส่ตอนนี้ฟรี ใส่ทีหลังแพง

---

## 6. เพิ่มฟิลด์/ตัวกรองที่ขาด

| สิ่งที่ขอ | ทำไม |
|---|---|
| `requestId` (id ที่เสถียร) | ตอนนี้คีย์จริงคือ `module + docNo` ต้องต่อคีย์เอง และเอกสารเองยังบอกให้ dedupe ด้วย docNo |
| `updatedDate` | ตอบคำถาม "ใบไหนเพิ่งขยับ" / "ใบไหนค้างไม่มีใครแตะมา 2 อาทิตย์" — ทำ SLA และรายงานใบค้างไม่ได้เลยถ้าไม่มี |
| กรองด้วย `jobStatus` ตรง ๆ เช่น `jobStatus=3` หรือ `jobStatus=1,2` | ตอนนี้ `summary` ให้ตัวเลขแยกตามสถานะ แต่กรองได้แค่กลุ่มหยาบ Open/Closed → ผู้ใช้เห็น "อยู่ระหว่างดำเนินการ 23 ใบ" แล้วคลิกดูไม่ได้ |
| `attachmentCount` ใน list | จะได้ขึ้นไอคอนคลิปหนีบในตารางโดยไม่ต้องเปิดใบ |

---

## 7. เรื่องเล็กน้อย

1. **parameter ที่ใช้ไม่ได้ ควรตอบ 400 แทนการเมินเงียบ ๆ** — ตอนนี้ส่ง `onlyMyTurn` ไป `/outgoing` แล้วถูกเมิน ทำให้ตอน debug ไม่รู้ว่าพลาดตรงไหน
2. **ชื่อฟิลด์ให้สะกดแบบเดียวกันทั้งระบบ** — login ส่ง `departid` แต่ Requests API ใช้ `departId` ตอนนี้ frontend ต้องเขียนโค้ดรับทั้งสองแบบ
3. `POST /ITRequest` (ฟอร์มสร้างใบ) ยัง**ไม่คืนเลขที่ใบ** ที่ generate ให้ — ขอให้คืน `docNo` กลับมา จะได้แสดงบนหน้าจอ "ส่งเรียบร้อย" ว่าใบเลขอะไร
4. **แนบไฟล์** — ฟอร์มแนบรูปได้ 3 รูปแต่ยังส่งไม่ได้ เพราะ payload ไม่มีช่องรับไฟล์ (`POST /Requests/{module}/{docNo}/attachments`, multipart)

---

---

## 8. ⭐⭐ `phase` — ตัวนับที่เทียบข้ามแผนกได้

> ### ✅ ทำเสร็จแล้วใน API v2 (18 ส.ค. 2026)
> backend ส่ง `phase` / `phaseName` / `ownerType` / `phaseSummary` / `wfStepTotal` / `wfStepName`
> มาต่อ item แล้ว, `/outgoing` รับ `module` ว่างได้, และมี `?phase=` + `page`/`pageSize`
> → หน้าเว็บลบตาราง map `WFStatus → phase` ทิ้งแล้ว เหลือแค่สีใน `src/data/requestPhase.ts`
>
> **หัวข้อนี้เก็บไว้เป็นบันทึกเหตุผล** ว่าทำไมถึงห้ามนับด้วย `jobStatus`
> (§8.1) — อย่าลบทิ้ง และคำถามใน §8.4 บางข้อยังไม่ได้คำตอบ

### 8.1 ปัญหา

หน้า **"เรื่องที่แจ้งออกไป"** ต้องรวมทุกแผนกปลายทางไว้ตารางเดียว เพราะแผนกหนึ่งแจ้งไปได้ทุกแผนก
(PL แจ้งไป IT / SV / HR / AF / GA …) และคำถามในใจผู้ใช้คือ *"เรื่องที่แจ้งไปถึงไหนแล้ว"*
ไม่ใช่ *"เรื่องของแผนก IT"* — ถ้าบังคับเลือกแผนกก่อน ต้องเปิดทีละแท็บถึงจะเห็นครบ
และใบที่ค้างนานที่สุดมักอยู่แท็บที่ไม่ได้เปิด

สิ่งที่ผู้ใช้ขอคือการ์ดนับ: **รออนุมัติ / รอรับเรื่อง / กำลังดำเนินการ / รอประเมิน / รอปิดงาน**

**`jobStatus` ใช้นับข้ามแผนกไม่ได้** — ตรวจจากตาราง `BC_<แผนก>_WorkFlow` จริงแล้วพบว่า:

| ปัญหา | หลักฐาน |
|---|---|
| รหัสเดียวกัน คนละความหมาย | `SQA` JobStatus **1** = *รอรับเรื่อง* แต่ทุกแผนกอื่น **1** = *รอ Mgr อนุมัติ* |
| รหัสซ้ำในใบเดียวกัน | `PS` step 2 กับ 3 เป็น JobStatus **2** ทั้งคู่ / step 4 กับ 5 เป็น **3** ทั้งคู่ → นับซ้ำ |
| ความหมายไม่เท่ากัน | `IT` **3** = `Service` (เหลืออีก 2 ขั้น) แต่ `AF`/`PL` **3** = `Service And Close-Job` (ทำเสร็จแล้ว) |
| รหัส "ปิดงาน" ไม่ตรงกัน | workflow จบที่ JobStatus สูงสุดคนละตัว: SQA=2, GA/IM=3, PS=4, IT/AF/PL=5 |

### 8.2 ข่าวดี — ศัพท์กลางมีอยู่แล้วในฐานข้อมูล

คอลัมน์ **`WFStatus`** ในตาราง workflow ใช้ร่วมกันทุกแผนกอยู่แล้ว มีแค่ **11 ค่า**
แต่ละแผนกต่างกันแค่ *เลือกใช้ตัวไหนบ้าง* และ *เรียงลำดับยังไง* (2–6 ขั้น)

ขอให้ backend map `WFStatus` → `phase` ตามตารางนี้ (frontend ใช้ค่าเดียวกันนี้อยู่แล้ว):

| `WFStatus` | `phase` | ป้ายไทย |
|---|---|---|
| `Approved-Request` | `waiting_approve` | รออนุมัติ |
| `Receive-Request` | `waiting_accept` | รอปลายทางรับเรื่อง |
| `Service` | `in_progress` | กำลังดำเนินการ |
| `Service And Close-Job` | `in_progress` | กำลังดำเนินการ |
| `Survey` | `waiting_review` | รอตรวจรับ / ประเมิน |
| `Received-Service` | `waiting_review` | รอตรวจรับ / ประเมิน |
| `Mgr Close-Job` | `waiting_close` | รอปิดงาน |
| `ReceiveJob-Close-Job` | `waiting_close` | รอปิดงาน |
| `Close-Job` | `waiting_close` | รอปิดงาน |
| `Request-Close-Job` | `waiting_close` | รอปิดงาน |
| `Mgr Request-Close-Job` | `waiting_close` | รอปิดงาน |

ใบที่จบแล้วไม่มีขั้นตอน จึงเพิ่มอีก 3 ค่า: `closed` (ปิดงานแล้ว) / `cancelled` (ยกเลิก) /
`other` (map ไม่ได้)

> **`other` สำคัญ** — รหัสที่ยังไม่ได้ map **ห้ามเดาลง `closed` เด็ดขาด**
> นับใบที่ยังค้างเป็น "เสร็จแล้ว" คือความผิดพลาดที่ไม่มีใครจับได้
> ส่วนถัง `other` ที่มีเลขโผล่มา คนเห็นแล้วโทรถามทันที

### 8.3 สิ่งที่ขอให้เพิ่ม

**(ก) `phase` ต่อ item และใน `/workflow`**
```json
"items": [{ "...": "...", "phase": "waiting_review", "phaseName": "รอตรวจรับ / ประเมิน" }]
```
ใน `/Requests/workflow` (§1.1) เพิ่มลงไปในแต่ละ status ด้วย: `{ code, name, group, phase }`

**(ข) `ownerType` ต่อ item — ขั้นปัจจุบันเป็นของฝั่งไหน** `"requester"` / `"target"`

ในตาราง workflow `DepartId = "00"` เป็น placeholder ที่แปลว่า *"แผนกของผู้เปิดเรื่อง"*
หลาย workflow **วนกลับมาหาผู้แจ้งตอนท้าย** — เช่น PL: `00 → 20 → 20 → 20 → 00`
(`Request-Close-Job` รอผู้เปิดเรื่องปิดงาน)

→ ใบที่งานเสร็จหมดแล้ว **รอแค่ผู้แจ้งกดปิดเอง** จะค้างเงียบ ๆ เป็นเดือนโดยไม่มีสัญญาณอะไรเลย
หน้าเว็บต้องมีตัวกรอง "รอเราลงมือ" ซึ่งต้องใช้ค่านี้

> ⚠️ **อนุมานจาก `WFStatus` อย่างเดียวไม่ได้** — `PS` มี `Approved-Request` ทั้งที่ step 1
> (`DepartId 00` = Mgr ผู้แจ้ง) และ step 3 (`DepartId 21` = Mgr ปลายทาง)
> จึงต้องให้ backend ที่รู้ `DepartId` ของขั้นนั้นเป็นคนบอก

**(ค) `module` ให้เป็น optional ใน `/outgoing`**

ตอนนี้ไม่ส่ง `module` = 400 ทำให้หน้าเว็บต้องยิงทีละโมดูลแล้วรวมผลเอง (N requests ต่อการเปิดหน้า 1 ครั้ง)
→ **ไม่ส่ง `module` = คืนทุกโมดูลที่แผนกนี้แจ้งออกไป** (ส่งมาก็ยังกรองเหมือนเดิม ไม่ breaking)

**(ง) `summary` ให้นับตาม phase และรวมข้ามโมดูล**
```json
"summary": [
  { "phase": "waiting_approve", "phaseName": "รออนุมัติ",       "count": 4  },
  { "phase": "in_progress",     "phaseName": "กำลังดำเนินการ",  "count": 12 }
]
```
ตัวเลขต้องมาจาก backend — ถ้าให้ frontend นับจากแถวที่โหลดมา พอใส่ pagination (§5)
การ์ดจะนับแค่หน้าปัจจุบัน **ตัวเลขจะโกหกโดยไม่มีใครรู้**

**(จ) กรองด้วย `phase`** — `GET /Requests/outgoing?phase=waiting_approve`
(รับหลายค่าคั่นด้วย comma ได้ยิ่งดี) ไม่มีตัวนี้ ผู้ใช้เห็น "รออนุมัติ 4 ใบ" แล้วคลิกดูไม่ได้

**(ฉ) `wfStepTotal`** (ตาม §1.2) — จำนวนขั้นทั้งหมดของใบนั้น
ตัวเลขขั้นเดี่ยว ๆ ตีความข้ามแผนกไม่ได้: "2" ของ SQA (จาก 2 = จบแล้ว) กับ "2" ของ PS (จาก 6 = เพิ่งเริ่ม)

### 8.4 คำถามที่ต้องการคำตอบ

1. **รหัสแผนก `11` และ `30` คือแผนกอะไร** — ที่ระบุได้แล้ว: `03`=AF, `06`=GA, `07`=IT, `18`=IM,
   `20`=PL, `21`=PS, `29`=SQA แต่ยังไม่เห็น **SV** กับ **HR** ที่ระบุไว้ในเอกสาร
2. **ใบที่ปิด/ยกเลิกแล้วใช้ JobStatus อะไรในแต่ละแผนก** — IT ใช้ `6`/`9` แต่ GA (workflow จบที่ 3)
   ปิดแล้วเป็นรหัสอะไร และ "ยกเลิก" ทุกแผนกใช้ `9` เหมือนกันไหม
3. **`SQA` ไม่มีขั้น `Approved-Request` เลย** → ขัดกับข้อตกลงข้อ 1 ใน `API_SPEC_REQUEST_FLOW.md`
   ที่ว่า *"ทุกใบต้องให้ Mgr อนุมัติก่อน"* — ตกลงยึดอันไหน
4. **`Active` มีเฉพาะตาราง IT กับ PL** — ตารางอื่นไม่มีการเปิด/ปิดขั้นตอนใช่ไหม
5. **ตาราง workflow เก็บเป็น TIS-620** — ถ้าอ่าน `Description` ส่งมาตรง ๆ ต้องแปลงเป็น UTF-8 ก่อน
   ไม่งั้นหน้าเว็บได้ตัวอักษรเพี้ยน
6. **ใครเป็นเจ้าของตาราง map `WFStatus` → `phase`** — ควรเป็นฝ่าย business เซ็นรับ ไม่ใช่ dev เดา
   โดยเฉพาะนิยามของ "เสร็จแล้ว": IT จบหลังผ่านขั้นประเมิน แต่แผนก 3 ขั้นจบทันทีที่ทำงานเสร็จ

---

## ลำดับที่อยากได้ (เรียงตามผลกระทบ)

1. **§8 `phase` + `ownerType`** — หน้า "เรื่องที่แจ้งออกไป" นับการ์ด KPI ข้ามแผนกไม่ได้เลยถ้าไม่มี
   (ตอนนี้หน้าเว็บ map เองไว้ชั่วคราวที่ `src/data/requestPhase.ts` — ลบทิ้งได้ทันทีที่ API ส่งมา)
2. **§8(ค) `module` เป็น optional** — ตัดการยิง N requests ต่อการเปิดหน้า 1 ครั้ง
3. **§1 workflow เป็นข้อมูล** — ปลดล็อกให้รองรับทุกแผนกโดยไม่ต้องแก้หน้าเว็บ
4. **§2 `availableActions`** — เหตุผลเดียวกัน แต่ฝั่งปุ่ม
5. **§3 action endpoint** — ทำให้เว็บใช้งานได้จริง ไม่ใช่แค่จอดู
6. **§5 pagination params** — ราคาถูกที่สุดตอนนี้ แพงที่สุดถ้าทำทีหลัง
7. **§4 detail endpoint** + **§6 `updatedDate` / กรองด้วย jobStatus**
8. §7 เรื่องเล็ก ๆ + แนบไฟล์

---

## 10. `requiredFields` — ขอ metadata ของฟิลด์มาด้วย

### ปัญหา

v2.0/v2.1 ส่ง `requiredFields` มาเป็น **ชื่อฟิลด์เปล่า ๆ**

```json
{ "code": "service", "label": "ดำเนินการ", "style": "primary",
  "requireNote": false, "requiredFields": ["repairStatus", "exVendor"] }
```

หน้าเว็บวาดฟอร์มไม่ได้ เพราะไม่รู้ว่า:

- `repairStatus` เป็น dropdown หรือช่องพิมพ์ — ถ้าเป็น dropdown ตัวเลือกมีอะไร
- `exVendor` คือฟิลด์อะไร (ไม่มีใน `resolution` ที่ API ส่งมา ซึ่งมีแค่
  `repairStatus` / `solution` / `resolutionDetail`)
- ป้ายภาษาไทยของแต่ละช่องคืออะไร
- ความยาวสูงสุด / รูปแบบที่ยอมรับ

**ตอนนี้หน้าเว็บจึงปิดปุ่มของ action ที่มี `requiredFields` ไว้** พร้อม tooltip
บอกเหตุผล — ดีกว่าเปิดให้กดแล้วส่งข้อมูลไม่ครบ

### สิ่งที่ขอ

เปลี่ยน `requiredFields` จาก `string[]` เป็น array ของ object:

```json
"requiredFields": [
  {
    "name": "repairStatus",
    "label": "สถานะการซ่อม",
    "type": "select",
    "required": true,
    "options": [
      { "value": "1", "text": "ซ่อมเสร็จ" },
      { "value": "2", "text": "รออะไหล่" }
    ]
  },
  {
    "name": "exVendor",
    "label": "ผู้รับเหมาภายนอก",
    "type": "text",
    "required": false,
    "maxLength": 100
  }
]
```

| ฟิลด์ | ใช้ทำอะไร |
|---|---|
| `name` | ชื่อที่ส่งกลับใน body ของ action |
| `label` | ป้ายไทยบนฟอร์ม |
| `type` | `text` / `textarea` / `select` / `number` / `date` / `checkbox` |
| `required` | บังคับกรอกไหม (บาง action มีฟิลด์ที่ไม่บังคับ) |
| `options` | เฉพาะ `type: "select"` |
| `maxLength` | ความยาวสูงสุด (กันยิงแล้วโดน 400) |

แล้ว body ตอนกดส่งเป็น:

```json
POST /Requests/IT/2600042/action
{ "action": "service", "note": null,
  "fields": { "repairStatus": "1", "exVendor": "" } }
```

### ทำไมต้องมาจาก backend

เหตุผลเดียวกับ `phase` และ `availableActions` เป๊ะ ๆ —
ถ้าให้ frontend เก็บตารางว่า "`repairStatus` เป็น dropdown มีตัวเลือกอะไรบ้าง"
ก็เท่ากับ hardcode ฟอร์มของทุกแผนกไว้ในหน้าเว็บ **เป็นรอบที่สาม**
(รอบแรก = รายการสถานะ, รอบสอง = ปุ่ม, รอบสาม = ฟอร์ม)
เปิดแผนกใหม่ที่มีฟิลด์ของตัวเอง = แก้โค้ด + deploy อีกครั้ง

ถ้าส่ง metadata มา หน้าเว็บวาดฟอร์มได้เองทุกแผนกตลอดไป

### คำถามที่ต้องเคลียร์

1. `exVendor` คือฟิลด์อะไร เก็บที่คอลัมน์ไหน
2. `note` ยังไม่มีคอลัมน์รองรับ — จะเพิ่มคอลัมน์ หรือทำตาราง log แยก
   (`ITRequestLog` ตาม `API_SPEC_REQUEST_FLOW.md` §2.2) ถ้าไม่เก็บตั้งแต่แรก
   ย้อนทำทีหลังไม่ได้ ตอนนี้หน้าเว็บส่ง `note` ไปแล้วแต่ API ละไว้
3. ค่า `approve` ในตาราง `Login` เป็นระดับ (1/2/3) หรือ flag (0/1) — กระทบ
   `WorkflowPermissions.CanAct` ที่แก้ไปใน v2.1

---

## 11. สิ่งที่ "หน้าอนุมัติ" ต้องการ

หน้าเว็บต่อ `POST .../action` เสร็จแล้ว ปุ่มขึ้นจาก `availableActions` ล้วน ๆ
หัวข้อนี้คือสิ่งที่**ยังขาด**เพื่อให้ Mgr ตัดสินใจอนุมัติได้จริง เรียงตามความสำคัญ

> **หมายเหตุสำคัญ:** ข้อ 2–3 หน้าเว็บ**ไม่ต้องแก้โค้ดเลย** — พอ backend ใส่ action
> ใหม่ลง `availableActions` ปุ่มจะขึ้นเองพร้อมกล่องกรอกเหตุผล เพราะทุกอย่างขับด้วยข้อมูล

---

### 11.1 🔴 บล็อกอยู่ตอนนี้ — `approve` เป็นระดับหรือ flag

ทดสอบจริงแล้ว **ปุ่มอนุมัติไม่ขึ้น** เพราะ `availableActions` ว่าง

| | ค่า |
|---|---|
| `Permission` ของขั้น 1 (`BC_IT_WorkFlow`) | 3 |
| `approve` ของ user ที่ทดสอบ | 1 |

`CanAct` ที่แก้ใน v2.1 ใช้ `userApproveLevel >= stepPermission` → `1 >= 3` เป็นเท็จ → ไม่มีปุ่ม

```sql
SELECT approve, COUNT(*) FROM Login GROUP BY approve
```

- เห็นแต่ `0`/`1` → เป็น **flag** ต้องแก้ `WorkflowPermissions.CanAct` (แก้ที่เดียว ใช้ทั้งปุ่มและตอนกดจริง)
- เห็น `1`/`2`/`3` → เป็น **ระดับ** ต้องแก้ค่า `approve` ของบัญชี Mgr เป็น 3

**ตราบใดที่ข้อนี้ยังไม่เคลียร์ หน้าอนุมัติใช้งานไม่ได้เลย**

---

### 11.2 🔴 ปุ่ม "ไม่อนุมัติ" — ขาดไม่ได้

ตอนนี้มีแต่ `approve` → **Mgr ที่กดได้แค่ "อนุมัติ" ไม่ได้เรียกว่าอนุมัติ เพราะไม่มีทางเลือก**
เจอใบที่ไม่สมควรอนุมัติ ต้องกลับไปกดในโปรแกรมเดิมอยู่ดี ซึ่งทำให้เว็บนี้ไม่มีประโยชน์กับ Mgr

```json
"availableActions": [
  { "code": "approve",     "label": "อนุมัติ",    "style": "primary", "requireNote": false, "requiredFields": [] },
  { "code": "not_approve", "label": "ไม่อนุมัติ", "style": "danger",  "requireNote": true,  "requiredFields": [] }
]
```

- **ชื่อ `code` เลือกเองได้เลย** (`reject` / `not_approve` / อะไรก็ได้) หน้าเว็บส่งกลับตามที่ได้รับ ไม่ได้ผูกกับชื่อ
- `style: "danger"` → หน้าเว็บใช้เลือกสีปุ่ม (แดง) ทุกปุ่มอยู่ในหน้ารายละเอียดอยู่แล้ว
  ไม่มีปุ่มลัดในตาราง — คนอนุมัติต้องเปิดใบอ่านก่อนเสมอ
- `requireNote: true` → กล่องกรอกเหตุผลบังคับ มีอยู่แล้ว ไม่ต้องทำเพิ่ม
- ต้องพาใบไปสถานะจบ (`phase: "cancelled"` หรือสถานะ "ไม่อนุมัติ" ของแผนกนั้น) และ `ownerType: null`

---

### 11.3 🔴 `note` ต้องเก็บลง DB ได้

ตอนนี้หน้าเว็บส่ง `note` ไปแล้วแต่ API ละไว้ เพราะไม่มีคอลัมน์รองรับ

**ถ้าไม่แก้ข้อนี้ ข้อ 11.2 ทำไม่ได้** — Mgr กด "ไม่อนุมัติ" พร้อมเหตุผล แล้วเหตุผลหายไป
ผู้แจ้งไม่มีทางรู้ว่าโดนตีกลับเพราะอะไร ต้องเดินไปถามเอง = แย่กว่าไม่มีปุ่ม

ทางเลือก:
- **(ก) ตาราง log แยก** ตาม `API_SPEC_REQUEST_FLOW.md` §2.2 (`ITRequestLog`) — แนะนำ ได้ timeline ในข้อ 11.5 ฟรี
- (ข) เพิ่มคอลัมน์ `ApproveNote` ใน `BC_IT_WFRequest` รายขั้น — เร็วกว่า แต่ไม่ได้ประวัติ

> ⚠️ log แบบ insert-only **ถ้าไม่เก็บตั้งแต่วันแรก ย้อนทำทีหลังไม่ได้** ข้อมูลที่ผ่านไปแล้วหายถาวร

---

### 11.4 🟡 ส่งฟิลด์ที่ผู้แจ้งกรอกมาให้ครบ

`POST /ITRequest` เก็บ 7 ฟิลด์ แต่ list ส่งกลับมาไม่ครบ:

| ฟิลด์ตอนสร้าง | มีใน list? | Mgr ต้องใช้ทำอะไร |
|---|:---:|---|
| `requestDetail` | ✅ (`detail`) | |
| `phoneNumber` | ❌ | โทรถามผู้แจ้งก่อนตัดสินใจ / ส่งต่อให้ช่าง |
| `comName` | ❌ | เครื่องไหน — ใบ IT ทั้งใบวนรอบเครื่องนี้ |
| `remark` | ❌ | หมายเหตุที่ผู้แจ้งเขียนเพิ่ม |

ของจริงที่เจอตอนทดสอบ: หน้ารายละเอียดแสดง **"รายละเอียดที่แจ้ง: 124"** เท่านั้น
Mgr ตัดสินใจจากข้อมูลเท่านี้ไม่ได้ — จะกดอนุมัติก็ไม่รู้ว่าอนุมัติอะไร

ทั้ง 3 ฟิลด์ **มีอยู่ใน DB แล้ว** แค่ไม่ได้ส่งออกมา

---

### 11.5 🟡 timeline — ใครทำอะไรไปแล้วบ้าง

ตอนนี้หน้ารายละเอียดสร้าง timeline จาก `resolution` ซึ่งขั้นแรกยังเป็น `null`
→ **แสดงได้แค่บรรทัดเดียวคือ "แจ้งเรื่อง"**

workflow ที่มีอนุมัติหลายรอบต้องเห็นของเดิม — เช่น `PS` มี `Approved-Request` 2 ขั้น
(step 1 Mgr ผู้แจ้ง, step 3 Mgr ปลายทาง) Mgr ขั้นที่ 3 ต้องรู้ว่าขั้น 1 ใครอนุมัติ เมื่อไร ด้วยเหตุผลอะไร

ขอ `GET /api/v1/Requests/{module}/{docNo}` (§4 เดิม) คืน:

```json
"logs": [
  { "step": 1, "action": "create",  "actionByName": "วิชิต เทียนทอง", "actionDate": "2026-08-18T17:17:00", "note": null },
  { "step": 1, "action": "approve", "actionByName": "หัวหน้าแผนก IT",  "actionDate": "2026-08-18T18:02:00", "note": null }
]
```

ข้อมูลบางส่วนมีอยู่แล้วใน `BC_IT_WFRequest` (`Approved` / `ApproveBy` / `ApproveDate` รายขั้น)
→ ทำ endpoint นี้ก่อนได้เลยโดยยังไม่ต้องมีตาราง log ใหม่

---

### 11.6 🟡 ไฟล์แนบ

ฟอร์มสร้างใบแนบรูปได้ 3 รูปแต่ **ส่งไม่ได้** เพราะ payload ไม่มีช่องรับไฟล์

ใบแจ้ง "คอมเปิดไม่ติด" ที่มีรูปหน้าจอ error กับใบที่ไม่มีรูป ต่างกันมากสำหรับคนอนุมัติ

- `POST /Requests/{module}/{docNo}/attachments` (multipart, ≤3 ไฟล์)
- `attachmentCount` ใน list → ขึ้นไอคอนคลิปในตารางโดยไม่ต้องเปิดใบ
- `attachments: [{ fileId, fileName, url }]` ใน detail endpoint

---

### 11.7 🟢 `updatedDate` — ใบไหนค้างนาน

Mgr ที่มีใบรออนุมัติ 20 ใบ ต้องรู้ว่าใบไหนค้างมา 3 วันแล้ว
ตอนนี้เรียงได้แค่ `requestDate` ซึ่งไม่ใช่ "เวลาที่ค้างอยู่ขั้นนี้"

ขอ `updatedDate` ต่อ item (ซ้ำกับ §6 เดิม) → หน้าเว็บทำป้าย "ค้าง 3 วัน" สีแดงได้

---

### 11.8 🟢 อนุมัติหลายใบพร้อมกัน (ถ้ามีเวลา)

Mgr เปิดมาเจอ 20 ใบรออนุมัติ ต้องกดยืนยันทีละใบ 20 รอบ

```
POST /api/v1/Requests/{module}/actions
{ "action": "approve", "docNos": ["2600045","2600046"], "note": null }
→ { "succeeded": [...], "failed": [{ "docNo": "...", "message": "รายการนี้อนุมัติไปแล้ว" }] }
```

ต้องคืนผลรายใบ เพราะบางใบอาจถูกคนอื่นกดไปก่อนระหว่างนั้น
**ทำทีหลังได้** ไม่ใช่ตัวบล็อก

---

### สรุปลำดับ

| # | เรื่อง | ทำไมสำคัญ |
|---|---|---|
| 1 | **11.1** `approve` ระดับ/flag | ตอนนี้ปุ่มไม่ขึ้นเลย ใช้งานไม่ได้ |
| 2 | **11.3** เก็บ `note` | เป็นเงื่อนไขของข้อถัดไป และย้อนทำไม่ได้ |
| 3 | **11.2** ปุ่มไม่อนุมัติ | Mgr ที่เลือกไม่ได้ ไม่ใช่การอนุมัติ |
| 4 | **11.4** `phoneNumber` / `comName` / `remark` | มีใน DB แล้ว แค่ส่งออกมา — ถูกที่สุด |
| 5 | **11.5** timeline | workflow หลายขั้นต้องเห็นของเดิม |
| 6 | 11.6 ไฟล์แนบ · 11.7 `updatedDate` · 11.8 อนุมัติหลายใบ | |
