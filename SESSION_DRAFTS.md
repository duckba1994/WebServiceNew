# คืนหน้าจอและร่างเมื่อเซสชันหมดอายุ

ใช้กับใบแจ้งเรื่องทุกแผนก: หน้าสร้าง หน้าแก้ไข ช่องกรอกใน workflow และแบบฟอร์ม action

## พฤติกรรม

- เก็บข้อความ ตัวเลือก วันที่ รายการย่อย แผนก เลขที่ใบ และแท็บที่เปิด ใน `sessionStorage` ของแท็บนั้น พร้อมสำเนาในหน่วยความจำ
- เมื่อ API ตอบ 401 หรือ ProtectedRoute พบ JWT หมดอายุ จะตรึงร่างก่อนออกจากหน้า
- Login บัญชีเดิมจะกลับ URL เดิม และอ่านสถานะ/สิทธิ์ล่าสุดจาก API ก่อนใช้ปุ่มดำเนินการ
- Logout ด้วยปุ่ม หรือ login เป็นอีกบัญชี จะล้างร่างเดิมและเข้าหน้าหลัก
- ไม่เก็บ File/Blob, token, password, DOM, pending flags หรือการยืนยันคำสั่ง และไม่ส่งคำสั่งซ้ำเอง
- เก็บร่างได้ในแท็บเดิม ไม่ใช่การบันทึกลงฐานข้อมูลหรือการซิงก์ข้ามเครื่อง; ร่างหมดอายุหลังไม่มีการอัปเดต 12 ชั่วโมง
- หาก browser storage เขียนไม่ได้ ใช้สำเนาในหน่วยความจำสำหรับ login ในแท็บเดิมได้ แต่ไม่รับประกันการคืนหลังรีเฟรช
- ปิดฟอร์มตามปกติจะทิ้งข้อมูลของฟอร์ม; ออกจากระบบเพราะหมดอายุจะเก็บไว้

## เพิ่มหน้าใหม่

`ProtectedRoute` มี DraftScope ตาม pathname อยู่แล้ว สำหรับฟอร์ม/เอกสารที่เปลี่ยนตัวตนภายในหน้า ให้เพิ่มขอบเขตที่มี key ชัดเจน:

```tsx
<DraftScope key={`${module}::${docNo}`} name={`${module}::${docNo}`}>
  <MyForm />
</DraftScope>
```

ในฟอร์มให้ใช้ hook กลางแทน useState เฉพาะค่าที่ผู้ใช้กรอกหรือหน้าจอที่ต้องการคืน:

```tsx
const [form, setForm] = useSessionDraft('MyForm.form.v1', emptyForm);

// ผู้ใช้พิมพ์/เลือก → เก็บร่างทันที
setForm(previous => ({ ...previous, remark: value }));

// โหลดค่าเริ่มต้นจาก API → เติมเฉพาะก่อนผู้ใช้แก้ไข ห้ามทับร่างที่กู้คืน
useEffect(() => {
  if (doc) setForm.hydrate(toForm(doc));
}, [doc, setForm]);

// เมื่อบันทึกสำเร็จหรือกดยกเลิก จึงเปลี่ยนเป็นค่าที่สะอาด
setForm.reset(toForm(savedDoc));
```

ข้อควรระวัง:

1. ชื่อ state ต้องไม่ซ้ำกันภายใน scope; ถ้าเปลี่ยนรูปแบบข้อมูลให้เปลี่ยนเวอร์ชันในชื่อ
2. ใช้ scope + React key ใหม่เมื่อเปลี่ยนเอกสาร; ห้ามนำ state ใบหนึ่งไปใช้กับอีกใบ
3. เก็บเฉพาะข้อมูลฟอร์ม ไม่ใช้ร่างตัดสินสิทธิ์หรือสถานะ workflow; โหลดข้อมูลเหล่านั้นใหม่เสมอ
4. ระหว่างสิทธิ์ยังโหลดอยู่ ให้ล็อกการบันทึก แต่อย่าเพิ่งล้างร่างเพราะ `canEdit` ยัง undefined
5. ยืนยัน/POST/PUT ต้องมาจากการกดของผู้ใช้เสมอ ห้ามเรียกใน effect เพื่อส่งซ้ำหลัง login
6. `setter.restored` ใช้ข้าม default-first ที่จะทำให้ช่องที่ผู้ใช้ตั้งใจล้างกลับมีค่า
7. หลีกเลี่ยง reset effect ตอน mount ที่ล้างค่ากู้คืน; reset เฉพาะเมื่อเอกสารเปลี่ยนหรือผู้ใช้สั่งจริง
8. เมื่อปิดเอกสารหรือยกเลิก action ที่ยังโหลดไม่เสร็จ ใช้ `useDiscardSessionDraft()` ล้าง scope รวมถึงค่าที่กู้มาแต่ยังไม่ได้ mount

ไฟล์หลัก: `src/utils/sessionDrafts.ts`, `src/hooks/useSessionDraft.tsx`, `src/context/AuthContext.tsx`.
